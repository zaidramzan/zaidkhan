how to create github account 

add repo 

ak command add ki ha  ssh-keygen -t rsa -b 4096 -C "your_email@example.com"


c/user/link/.ssh

file ko save ki ha publickey and privatekey

file ko git clone kai ha 

ssh ma public key ko github ma add ki ha 

cmd ma desktop ma run ki ha  cd Desktop

cmd ma git clone  git@github.com:zaidramzan/zaidkhan.git

opened the folder in vs code 

open in terminal vs code and execute the command 


      1       git add .

      2        git commit -m "massage add karna ha"

      3         git push 